{
  "name": "redux-resx",
  "version": "1.0.0",
  "description": "(Yet another) Redux action creators, a reducer and middleware for resource-based APIs",
  "main": "index.js",
  "repository": "https://github.com/fixate/redux-resx",
  "author": "Stan Bondi<stan@fixate.it>",
  "license": "MIT",
  "scripts": {
    "compile": "babel --presets es2015,stage-0 -d . src/",
    "prepublish": "npm run compile",
    "watch": "watch -p '**/*.js' -c 'npm run compile'",
    "test": "babel-tape-runner test/**/*_test.js",
    "clean": "rm *.js"
  },
  "devDependencies": {
    "babel-cli": "^6.24.0",
    "babel-preset-es2015": "^6.24.0",
    "babel-preset-stage-0": "^6.22.0",
    "babel-tape-runner": "^2.0.1",
    "sinon": "^2.1.0",
    "tape": "^4.6.3",
    "watch-cli": "^0.2.1"
  },
  "peerDependencies": {
    "redux": "3.x"
  }
}
